export function createInput(){const keys=new Set();const t={x:0,y:0,active:false};addEventListener("keydown",e=>{keys.add(e.key.toLowerCase());if([" ","arrowup","arrowdown","arrowleft","arrowright"].includes(e.key.toLowerCase()))e.preventDefault()});addEventListener("keyup",e=>keys.delete(e.key.toLowerCase()));const joy=document.querySelector("#joy"),stick=document.querySelector("#stick");function move(e){const r=joy.getBoundingClientRect(),x=e.clientX-r.left-r.width/2,y=e.clientY-r.top-r.height/2,l=Math.min(45,Math.hypot(x,y)),a=Math.atan2(y,x);t.x=Math.cos(a)*l/45;t.y=Math.sin(a)*l/45}joy.addEventListener("pointerdown",e=>{t.active=true;joy.setPointerCapture(e.pointerId);move(e)});joy.addEventListener("pointermove",e=>t.active&&move(e));joy.addEventListener("pointerup",()=>{t.active=false;t.x=t.y=0;stick.style.transform=""});return{vector(){let x=(keys.has("d")||keys.has("arrowright")?1:0)-(keys.has("a")||keys.has("arrowleft")?1:0),y=(keys.has("s")||keys.has("arrowdown")?1:0)-(keys.has("w")||keys.has("arrowup")?1:0);if(t.active){x=t.x;y=t.y;stick.style.transform=`translate(${x*45}px,${y*45}px)`}const l=Math.hypot(x,y)||1;return{x:x/l,y:y/l,raw:Math.hypot(x,y)>0}},dash:()=>keys.has(" ")}}